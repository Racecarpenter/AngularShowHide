import { FilterythingPipe } from './filterything.pipe';

describe('FilterythingPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterythingPipe();
    expect(pipe).toBeTruthy();
  });
});
